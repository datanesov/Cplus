#include <iostream>
using namespace std;

int main() {
    float a, b, avg;
    cout << "Enter two numbers: ";
    cin >> a >> b;
    avg = (a + b) / 2;
    cout << "Average of the two numbers is: " << avg << endl;
    return 0;
}

