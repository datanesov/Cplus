#include <iostream>
#include <cmath>
using namespace std;

int main() {
    float a,y;
    cout << "Введите значение a: ";
    cin >> a;
    y = (pow(a, 2) + 1) / sqrt(pow(a, 2) + 1);
    cout << "Значение функции y = (a^2 + 1)/sqrt(a^2 + 1) равно " << y << endl;
    return 0;
}
